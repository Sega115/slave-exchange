{% extends 'slaveList/index.html.twig' %}
{% block content %}
    {% for slave in slaves %}
        <p><a href="{{ path('slave-info', {'slaveID' : slave.id})}}">{{ slave.name }}</a></p>
    {% endfor %}
{% endblock %}